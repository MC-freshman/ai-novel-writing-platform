# AI小说创作平台 使用说明

## 直接使用

双击 `打开AI小说创作平台_新版.bat`，或打开 `release/AI小说创作平台 0.3.1.exe`。

软件首次运行会在系统“文档/AI小说创作平台/默认小说项目”下创建你的小说项目。这个项目目录保存正文、角色卡、世界观、向量索引和 API 设置，不会被源码仓库或发布压缩包打包进去。

## API 密钥

API 密钥只在软件内的项目设置中填写。Windows 正式版会把聊天密钥和向量密钥保存到 Windows 凭据管理器，项目配置只保存凭据引用；源码仓库、发布包和项目交换包均不包含你的 API 密钥。

通义千问可在“接口提供商”里选择“通义千问 / Qwen”，接口地址会自动填为 `https://dashscope.aliyuncs.com/compatible-mode/v1`，聊天模型默认 `qwen-plus`。

## AI 功能

- 右侧 AI 助手分为“对话”和“创作参谋”：对话负责自由问答，创作参谋负责下一章建议、剧情推进方案和伏笔建议。
- 右侧 AI 对话会在当前项目内保留最近 10 个会话；“项目 AI 记忆”可保存跨会话背景和偏好，不会跨项目共享。
- AI 回答会边生成边显示；可点击“停止生成”，停止或连接中断时已经收到的内容不会丢失，软件意外退出后也会尝试恢复最后一次生成断点。
- 创作参谋会根据目标自动选择章节规划、剧情因果、人物发展、伏笔、节奏、连续性、设定核验和安全修订工具。展开“Agent 权限与选区”可选择自动推荐、当前章节、当前分卷或全书范围，也可选择只读、创建规划或生成待确认修订；任何权限都不会直接覆盖正文。执行前仍需确认计划、资料范围和预计 Token。
- 点击右侧 AI 标题栏的波形图标可打开“创作状态”。剧情事实、角色状态和伏笔可按分卷、文档、状态筛选；剧情事实和伏笔先作为 AI 候选，作者可确认、忽略、标记强化/待回收/已回收，也可人工新增、编辑备注和内容。
- “角色状态”按章节记录人物最后位置、身体状态、目标、知情范围和持有物；重新分析会清除已经不在原文中的旧状态。
- “下一章筹备”可生成目标、承接、人物表现、冲突、信息、伏笔和结尾项目，支持新增自定义项、拖动、编辑、锁定和完成。参谋建议进入筹备板后会保留来源依据；AI 不直接写入正文。
- 点击底部状态栏的任务数量可打开后台任务中心。任务可暂停、继续、停止或重试；停止 AI 任务时，已经收到的部分输出会保留。Agent 每完成一个工具都会保存阶段结果，失败工具可在右侧参谋区单独重试。
- “快照与分支”可以先比较文件差异再恢复，也可改名、安全删除和清理无引用数据。恢复会先自动保存安全快照；被分支使用的快照、主线和当前分支不能删除。切换分支后软件会自动在后台重建知识库。
- “创作状态 / 创作工作台”包含七个紧凑页签：规划、脉络、修订、批注、记忆、诊断、项目交换。
- “规划”按场景记录视角、地点、目标、冲突、转折和结果，场景可拖动调整顺序。
- “脉络”可从剧情事实和伏笔重建剧情节点，作者可添加并锁定节点、关系和角色弧线。锁定内容不会被下次自动整理覆盖。
- “修订”只生成候选。选中正文后右键选择“生成安全修订”可直达；可整体采纳，也可展开“逐句选择”只采纳某一句。每次采纳前都会自动保留历史版本，正文变化或位置不明确时会停止写入。
- “批注”独立于正文保存；选中正文后右键选择“添加批注”可直达。正文版本变化后，旧批注会显示待核对提醒。
- “记忆”支持全书、分卷和章节三层范围，创作参谋按当前章节自动选用。
- “诊断”使用本地数据检查章节篇幅、剧情事实、角色变化、场景冲突和场景结果，不调用 AI，也不会修改正文。
- “项目交换”可导出或合并创作资料。导出时可设置密码；导入前先解锁并预览数量、重名项，再选择正文、角色卡、世界观、素材和分析数据。确认后自动创建快照，重名内容作为副本导入；交换包不包含模型设置、API 密钥、向量库和备份。
- “诊断”可生成创作统计，查看章节节奏、视角占比、角色出场密度、伏笔回收和角色弧线提醒；每次结果会保存，可对比修改前后变化。
- “脉络”上方的剧情图可切换因果图、伏笔图、人物弧线和势力图。节点可拖动，滚轮缩放，连线可编辑类型、颜色和方向。
- “角色”界面可从大纲生成角色卡片，并按分类折叠。
- “世界”界面可从大纲生成世界观条目，并按分类折叠。
- “分析”界面可提取地点、势力、物品条目，并直接写入“世界”界面。
- “知识库”界面可把导入文档整理为大纲、正文或补充材料，并同步所属目录；顶部折叠区可检查同步状态、补齐未同步资料、检查章节健康，以及查看并修复检索缓存、过期资料和 Agent 任务引用。
- 角色卡片和世界观条目都可以拖动到其他分类标题下。
- 设置里的“候选扫描上限（最高 50000）”控制本地初筛规模，“发送片段上限（最高 1000）”控制最多交给 AI 的原文片段数。两者都只是上限，实际数量会按问题类型、分层摘要、相关度和字数预算动态调整。
- 展开 AI 回答下方的“检索详情”，可查看自动拆分的子问题、首轮与第二轮补证数量、证据置信度、每卷原文与摘要覆盖、资料入选原因以及目录存在但未读原文的文档。

## 分析工作台

点击左侧“分析”后，可以使用：

- 全局搜索：搜索章节、角色卡、世界观。
- 创作参谋：根据当前文档、大纲、正文和知识库检索片段，整理下一章建议、剧情推进、伏笔建议。
- 时间线：可用 AI 识别真实剧情事件，也可用本地规则整理；事件可以拖动调整顺序。
- 关系网：根据角色关系栏和正文同场出现生成角色关系图，可滚轮缩放、拖动画面，可按角色分类或搜索筛选角色，并添加关系类型。
- 一致性：结合 AI 和本地规则检查设定矛盾，可限定审查章节和参考资料，问题可标记为已确认、已忽略、已修复。
- 版本对比：保存章节后自动记录保存前版本，可与当前版本对比。
- 版本恢复：在历史版本详情中可恢复指定版本，恢复前的当前内容也会先备份。
- 撤销和重做只作用于当前章节。切换章节后会建立新的撤销历史；若保存内容与另一章整篇完全相同，软件会停止保存并在底部状态栏说明原因。
- 导出/提取：按目录树逐篇导出 Word，每章正文、每份大纲和每份材料分别生成一个 `.docx`；可选择是否附带角色卡、世界观。提取地点、势力、物品时会先生成候选，勾选后再写入。
- 试验功能：人物出场统计、地点/势力版图、素材库放在“导出/提取”里的可折叠小卡片中。

时间线、关系网、一致性检查、提取候选和试验功能会保留上一次结果；只有点击刷新或开始检查时才会重新分析。

## 导入和重建进度

批量导入时底部会显示当前处理的文件和进度，可点击“取消”。取消会在当前文件处理完成后停止后续文件。重建知识库时会显示整理阶段和预计片段数。

批量导入结束后的剧情事实整理会转入后台任务，不会继续占住导入窗口。保存单章时只更新当前文档的知识库分片和创作状态。

## 编辑辅助

正文右键菜单支持向 AI 提问、改写、润色、扩写、总结、提取设定、生成安全修订和添加批注。编辑器顶部可打开查找替换，并显示当前选区字数。

正文旁的黄色标记代表批注，红色删除线标记代表待确认修订。点击标记可查看说明，再进入“创作状态 / 创作工作台”处理。

设置底部的“发布、密钥与软件更新”可查看 Windows 凭据保存状态、手动检查新版本、下载更新和运行发布隐私扫描。更新只会在你确认后下载与打开，不会自动安装。

## 导入和整理文档

点击“导入文档”时可以一次选择多个 `.docx`、`.txt`、`.md` 文件。导入完成后，左侧目录树中可以直接拖动文档：

- 拖到某个分组标题上：移动到该分组末尾。
- 拖到某个文档上：移动到该文档前面，并自动归入该文档所在分组。
- 点击分组标题右侧的导入按钮：直接把选择的多个文档导入到该分组下。

## 源码开发

安装依赖：

```powershell
npm install
```

开发运行：

```powershell
npm run start
```

重新打包 Windows 软件：

```powershell
npm run dist
```
