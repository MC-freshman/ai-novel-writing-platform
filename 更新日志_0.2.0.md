# AI小说创作平台 0.2.0 更新日志

## 创作 Agent

- 创作参谋升级为“准备计划 -> 作者确认 -> 执行”的两步 Agent。
- 执行前显示工具步骤、资料范围、分层记忆、预计 Token 和检索警告。
- 执行后保留检索审计与参谋历史；建议仍只进入筹备板或素材，不直接写正文。

## 创作工作台

- 在原有“创作状态”弹窗内新增工作台，不增加左侧导航项目。
- 场景规划支持目标、冲突、转折、结果和拖动排序。
- 剧情事实与伏笔可整理为剧情脉络，支持手工节点、自定义关系和锁定保护。
- 角色状态可整理为角色弧线，支持手工补充与锁定。
- 新增本地章节质量诊断，不调用 AI，不修改正文。

## 安全修订与批注

- 选中正文后可从右键菜单生成安全修订或添加批注。
- AI 修订先生成候选，必须逐条确认；采纳前校验章节版本和原文唯一位置。
- 采纳前自动保存章节历史版本；冲突或原文变化时停止写入并标记失效。
- 批注独立保存，正文变化后提示作者重新核对。

## 项目记忆与交换

- 项目记忆扩展为全书、分卷、章节三层，Agent 按当前章节自动选择。
- 新增项目交换包：导入前预览、导入前快照、冲突改名保留。
- 交换包不包含 API 设置、密钥、向量库、历史备份和本机导入路径。

## 数据兼容

- 0.1.35 项目首次打开 0.2.0 时会自动创建 `analysis/creative-workspace/state.json`。
- 原有章节、角色卡、世界观、知识库、剧情状态、快照和任务历史保持原目录，不需要手工迁移。
