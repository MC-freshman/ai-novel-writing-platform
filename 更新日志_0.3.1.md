# AI小说创作平台 0.3.1 更新日志

## 跨章节撤销修复

- 每个章节使用独立的富文档编辑器实例和撤销/重做历史，切换章节后 `Ctrl+Z` 不再回到上一章内容。
- 编辑器更新事件携带章节编号，已经卸载的旧编辑器不能修改当前章节。
- 快速连续切换章节时只采用最后一次加载结果，迟到请求不会覆盖当前编辑区。
- 章节局部状态与应用总状态同步更新，目录高亮、标题和正文保持同一章节身份。

## 保存保护

- 前端保存使用单一进行中请求，连续快捷键、自动保存和切章会等待同一保存结果。
- 后端按项目串行处理章节保存，并在队列中重新校验正文修订号。
- 章节正文改用临时文件写入后原子替换，降低中途退出或并发写入导致文件损坏的风险。
- 当一次保存准备把某章整篇替换成另一章已经存在的相同长文本时，软件会停止保存并保留原文与历史版本。
- 请求不存在的章节时直接报错，不再回退到目录中的第一章。
- 历史版本按正文内容去重，连续撤销产生的重复内容不会挤占版本保留名额。

## 验证

- 通过跨章节连续撤销的真实 Electron 界面复现测试。
- 通过并发保存、旧修订号、整章误覆盖、无效章节加载和版本去重数据回归。
- 通过完整构建、核心数据、Agent、快照、知识库、逐篇 Word 导出及 UI 按钮回归。
